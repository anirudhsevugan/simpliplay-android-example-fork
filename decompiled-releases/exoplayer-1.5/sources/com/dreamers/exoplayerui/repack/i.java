package com.dreamers.exoplayerui.repack;

import java.util.Arrays;

public enum i {
    SimplePlayerView,
    StyledPlayerView;

    public static i[] a() {
        return (i[]) Arrays.copyOf(c, 2);
    }
}
